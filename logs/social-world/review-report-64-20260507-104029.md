## 审查结论

审查结论: NEEDS_MAJOR_FIX

## 阻塞问题（Blocking）

无。

## 严重问题（Major）

### 1. 模板交付前检查清单未完整执行：`make test` 与 `make lint` 未运行
- **位置**: PR #64 Body「自检清单」
- **问题**: 开发者自检清单中以下两项仍标记为未执行：
  - `[ ] make test - ⚠️ 后端测试因环境缺失 ts-jest 无法运行`
  - `[ ] make lint - ⚠️ ESLint v9 与项目 .eslintrc.js 不兼容`
- **影响**: 无法确认后端单元测试、BDD 测试和静态代码检查是否全部通过，存在未暴露的回归风险。
- **修复**: 在开发环境完成依赖安装后，逐条运行并确认通过：
  ```bash
  make test
  make test-bdd
  make lint
  make check-contract-sync
  ```
修复所有报错后更新 PR 自检清单为 `[x]`。
- **依据**: `docs/exec-plans/templates/TEMPLATE-fullstack-feature.md`「交付前检查清单」明确将 `make test`、`make test-bdd`、`make lint`、`make check-contract-sync` 列为强制项。
- **上一轮状态**: ❌ 未修复

## 轻微问题（Minor）

1. **`ImBloc` 断连/离线时丢失分页状态**
   - **位置**: `apps/mobile/lib/presentation/blocs/im/im_bloc.dart:155-167`, `210-222`, `290-298`
   - **问题**: 当状态为 `MessageSending` 时，若触发 `_onImDisconnectRequested`、`_onSocketDisconnected` 或 `_onSendMessage`（离线），回退到 `MessagesLoaded` 时 `hasMore` 被硬编码为 `false`，`nextCursor` 被置空，导致已加载的历史分页状态丢失。
   - **建议**: 在 `MessageSending` 状态中额外缓存 `hasMore` 和 `nextCursor`，或在降级时尝试从最近一次 `MessagesLoaded` 快照恢复分页字段。

2. **`apps/api/src/modules/im/im.service.ts:52` 类型绕过**
   - `where: { user_id: Not(userId) as unknown as string }` 使用 `as unknown as string` 绕过 TypeORM 类型检查，建议改用 TypeORM 提供的正确类型写法或添加注释说明。

3. **`ImBloc` 注册为 `LazySingleton` 而非 Factory**
   - `apps/mobile/lib/core/di/di.dart` 将 `ImBloc` 注册为 `LazySingleton`。虽然 IM 状态全局管理有其合理性，但 BLoC 通常应为 Factory 作用域，以避免状态在页面间意外残留。

4. **`ImSocketClient.connect()` 未释放旧 Socket**
   - `apps/mobile/lib/core/network/im_socket_client.dart:37-49` 每次调用 `connect()` 都会创建新的 `io.Socket`，但未先 `disconnect()` 或销毁旧的 `_socket`，可能导致旧连接的事件监听器泄漏。当前由 `ImBloc` 在外部调用 `disconnect()` 兜底，但客户端本身应增加防御性清理。

## 上一轮问题修复状态

| # | 问题标题 | 上一轮级别 | 修复状态 | 说明 |
|---|---------|-----------|---------|------|
| 1 | BDD 离线推送测试断言了未与实际代码集成的 Mock | Major | ✅ 已修复 | `push.service.ts` 已创建骨架实现；`im.service.ts` 在 `sendMessage` 中调用 `this.pushService.send(...)`；`bdd-setup.js` 已在 NestJS 测试模块 `providers` 中注入 `PushService` 并将 `mockPushService` 作为 `useValue`；`getServer()` 返回 `pushService` |
| 2 | 模板交付前检查清单未完整执行 | Major | ❌ 未修复 | PR Body 自检清单仍标记 `[ ] make test` 和 `[ ] make lint` |
| 3 | 系统消息创建后未通过 Socket.io 实时通知 | Major | ✅ 已修复 | `im.service.ts:242` 在 `createSystemMessage` 保存成功后通过 `eventEmitter.emit("system_message:created", ...)` 发射事件；`im.gateway.ts:214-229` 通过 `@OnEvent("system_message:created")` 监听并向双方 Room 广播 `message:receive` 与 `conversation:updated` |
| 4 | Widgetbook 未注册新 IM 组件用例 | Major | ✅ 已修复 | `packages/design-system/widgetbook/main.dart` 与 `packages/design-system/lib/widgetbook/main.dart` 均已注册 `SwChatBubble`、`SwChatInputBar`、`SwConversationListItem`（Molecules）以及 `SwChatMessageList`（Organisms）的 `WidgetbookComponent` 与 `WidgetbookUseCase` |
| 5 | 文档与代码实现不一致 | Major | ✅ 已修复 | `docs/modules/im.md:77` 已将描述更新为「发送消息后必须更新 `conversations.updated_at`」，与 `im.service.ts:121-125` 的代码实现一致 |

## 修复引入的新问题

无。

## 验证结果

| 维度 | 状态 | 说明 |
|------|------|------|
| 功能完整性 | ✅ | 核心 IM（发送/接收/已读/会话列表）已实现；离线推送骨架（`PushService` + BDD Mock 集成）已完成；系统消息实时广播链路（`eventEmitter` + `ImGateway`）已贯通 |
| 代码规范性 | ✅ | 命名符合项目规范；无硬编码魔法值；Entity 已在 `ImModule` 的 `TypeOrmModule.forFeature()` 中注册；`data-source.ts` 通过 glob 覆盖新 Entity；事务内提交后网关再发射 Socket 事件 |
| 全链路完整性 | ✅ | DB（Migration + Entity）→ API（Controller/Gateway/Service/PushModule）→ Mobile（Page/BLoC/Repository/Socket）链路已贯通；`shared-types` 与 `mobile` 生成模型已同步 |
| 规范符合性 | ✅ | Widgetbook 已按执行计划注册新组件用例；`docs/modules/im.md` 与代码实现已一致 |
| 模板检查清单执行 | ❌ | `make test`、`make lint` 未执行；PR Body 自检清单仍有两项未勾选 |
| 测试覆盖 | ✅ | 后端单元测试（`im.service.spec.ts`）、Flutter BLoC 测试（`im_bloc_test.dart`）、Golden 测试、BDD 步骤均非空实现且 assert 了具体状态转换值；BDD 推送 scenario 已与实际代码集成 |
| 文档同步 | ✅ | `docs/api/im.md`、`docs/api/socket-events.md`、`docs/api/INDEX.md`、`docs/api-contracts/im.yaml`、`docs/modules/im.md` 均已更新 |

## 自检结果

- State 不可变性：✅ 通过（`ImBloc` 所有状态迁移均显式传递前序状态字段，未传入字段使用构造函数默认值；`Message.copyWith` 正确使用 `?? this.field` 回退）
- 生命周期：✅ 通过（`ImBloc.close()` 正确断开 Socket；`ImSocketClient.disconnect()` 置空 `_socket`；`ImBloc._onImConnectRequested` 先 `disconnect()` 旧客户端再创建新实例）
- 边界路径：✅ 通过（`ImService.sendMessage` 处理重复 `client_message_id`、非成员、超长内容；`ImService.getMessagesSince` 空 `conversationId` 返回 `[]`；`ImBloc._onLoadMessages` 防止并发分页请求；`ImGateway.handleConnection` 无 token 时断开连接）
- 回归：✅ 通过（未发现影响未修改文件预期行为的变更；`MatchingService` 新增 `match:created` 事件发射属于新功能路径）
- 测试真实性：✅ 通过（BLoC 测试 assert 了具体状态转换值如 `messages.length`、`status`、`sequenceId`；后端单元测试 assert 了返回值、Redis 调用参数、Repository 调用参数；BDD 步骤非空实现）

## 风险与建议

1. **本 PR 涉及状态管理与 Socket 生命周期变更，建议合并前人工确认边界场景**：特别是 `ImBloc` 在页面重建时作为 `LazySingleton` 可能导致旧状态残留；以及 `MessageSending` 状态降级为 `MessagesLoaded` 时分页字段丢失的问题。

2. **Redis 故障导致数据不一致**: `ImService.sendMessage` 中 Redis `incr` 位于数据库事务之外，且无独立的 try-catch。若 Redis 在事务提交后故障，`incr` 会抛出异常，发送方收到 `message:error`，但消息已持久化且接收方下次加载时仍能看到该消息（只是未读数未增加）。建议将 Redis 操作纳入错误降级逻辑，而非直接抛异常。

3. **消息同步过滤自身消息**: `ImService.getMessagesSince` 通过 `.andWhere("m.sender_id != :userId", { userId })` 过滤了用户自己发送的消息。若用户从另一设备发送消息，当前设备重连后无法通过 `message:sync` 补拉到这些自消息。MVP 阶段可接受，但建议在 `docs/modules/im.md` 中明确记录此限制。

4. **未读数无过期/上限策略**: Redis 中 `unread_count:{user_id}:{conversation_id}` 只增不减（除已读时重置为 0），长期运行可能导致 Redis 中存在大量冷 key。建议后续增加过期策略或定期清理。

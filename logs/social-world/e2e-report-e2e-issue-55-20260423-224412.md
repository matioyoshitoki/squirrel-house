# E2E 测试报告

- **测试时间**: 2025-04-23 22:44
- **总体结果**: ❌ 失败
- **Exit Code**: 1

## 汇总

| 指标 | 数量 |
|------|------|
| Flow 总数 | 6 |
| ✅ 通过 | 3 |
| ❌ 失败 | 3 |
| 📸 截图 | 10 |

## Flow 详情

### ✅ Flow: auth/login
- **结果**: PASSED (耗时 ~17s)
- **步骤详情**: 
  - launchApp (clearState: true)
  - runFlow: includes/login-as-test-user.yaml
    - assertVisible: "欢迎使用 Social World" ✅
    - assertVisible: "手机号一键登录" ✅
    - tapOn: "手机号" → inputText: 13800138000 ✅
    - tapOn: "获取验证码" ✅
    - tapOn: "验证码" → inputText: 123456 ✅
    - hideKeyboard ✅
    - tapOn: "登录" ✅
- **说明**: 本 flow 为纯 UI  smoke 测试，不验证登录后的页面跳转，因此无需后端即可通过。

### ✅ Flow: devtools/open_drawer
- **结果**: PASSED (耗时 ~5s)
- **步骤详情**:
  - launchApp (clearState: true)
  - assertVisible: id: "dev_tools_toggle_button" ✅
  - tapOn: id: "dev_tools_toggle_button" ✅
  - assertVisible: "服务器地址" ✅
- **修复记录**: 将 `assertVisible: "开发工具"` 改为 `assertVisible: id: "dev_tools_toggle_button"`。Flutter `Semantics(label: ...)` 不会以普通 `text` 形式暴露给 Maestro，但 `resource-id`（即 Semantics identifier）可被稳定定位。

### ✅ Flow: devtools/edit_server_url
- **结果**: PASSED (耗时 ~36s)
- **步骤详情**:
  - launchApp (clearState: true)
  - tapOn: id: "dev_tools_toggle_button" ✅
  - assertVisible: "服务器地址" ✅
  - tapOn: id: "server_base_url_field" → inputText → tapOn: "保存配置" ✅ (循环 3 次)

### ❌ Flow: matching/swipe_recommendations
- **结果**: FAILED (耗时 ~35s)
- **失败原因**: `Assertion is false: id: mode_tab_nearby is visible`
- **根因分析**: **环境问题** — 当前未启动 E2E 后端服务（`localhost:3001` 无响应）。Flow 结构正确：先 `clearState` → `login-as-test-user` → 断言首页 `mode_tab_nearby`。但登录按钮点击后，由于后端 API 不可用，App 仍停留在登录页面，无法进入首页。
- **截图证据**: 失败截图显示登录页，手机号/验证码已填写，登录按钮可点击但无后端响应。
- **修复建议**: 无需修改 flow。运行前执行 `make e2e-env-up` 启动 mysql + redis + api 即可。

### ❌ Flow: matching/view_match_list
- **结果**: FAILED (耗时 ~35s)
- **失败原因**: `Element not found: Id matching regex: tab_messages`
- **根因分析**: **环境问题** — 与 `swipe_recommendations` 相同，缺少后端服务。`login-as-test-user` 子 flow 的 UI 交互全部完成，但登录 API 调用失败后 App 停留在登录页，导致底部 Tab `tab_messages` 不存在。
- **截图证据**: 失败截图显示登录页，与 swipe_recommendations 一致。
- **修复建议**: 无需修改 flow。需启动后端服务后再运行。

### ❌ Flow: profile/setup
- **结果**: FAILED (耗时 ~34s)
- **失败原因**: `Assertion is false: "完善资料" is visible`
- **根因分析**: **环境问题** — 缺少后端服务。本次测试前已修复 flow 结构：补充了 `runFlow: includes/login-as-test-user.yaml`（原 flow 在 `clearState: true` 后直接断言 "完善资料"，忽略了登录态被清空的问题）。修复后，登录 UI 交互正确完成，但因后端未启动，登录 API 失败，App 仍停留在登录页，无法跳转至 `/profile-setup`。
- **截图证据**: 失败截图显示登录页，手机号/验证码已填写。
- **修复记录**: 
  - **文件**: `.maestro/flows/profile/setup.yaml`
  - **修改**: 在 `assertVisible: "完善资料"` 之前增加 `- runFlow: includes/login-as-test-user.yaml`，使 flow 自包含（与 `swipe_recommendations`、`view_match_list` 保持一致）。
- **修复建议**: 无需进一步修改 flow。需启动后端服务后再运行。

## 自检清单

1. **当前功能是否有对应的 maestro flow 覆盖？**
   - 登录页 UI (`auth/login`) ✅
   - DevTools 抽屉 (`devtools/open_drawer`, `devtools/edit_server_url`) ✅
   - 推荐卡片滑动 (`matching/swipe_recommendations`) ✅
   - 匹配列表 (`matching/view_match_list`) ✅
   - 完善资料 (`profile/setup`) ✅
   - 以上 flow 均已存在，本次修复了其中 2 个过时/设计缺陷的 flow。

2. **每个失败的 flow，是否都查看了截图并分析了根因？**
   - ✅ 已查看全部失败截图（`open_drawer`、`swipe_recommendations`、`view_match_list`、`setup`），确认根因为：
     - `open_drawer`: Flow 过时（Maestro 无法通过 text 定位 Flutter Semantics label）→ 已修复
     - `swipe_recommendations` / `view_match_list` / `setup`: 环境限制（后端未启动）→ 无需修改 flow

3. **修改/创建的 flow 文件是否只限于 `.maestro/flows/` 目录？**
   - ✅ 是。仅修改了 `.maestro/flows/devtools/open_drawer.yaml` 和 `.maestro/flows/profile/setup.yaml`。

4. **报告中的修复建议是否具体可行？**
   - ✅ 是。给出了具体文件、具体修改内容（`open_drawer` 改用 id 断言；`setup` 增加 login 子 flow），并明确指出了后端启动命令。

## 相关路径

- 📸 Debug 目录: `/Users/insulate/Desktop/new-world/new-world-ops/logs/social-world/e2e-screenshots-e2e-issue-55-20260423-224412`
- 📄 报告文件: `/Users/insulate/Desktop/new-world/new-world-ops/logs/social-world/e2e-report-e2e-issue-55-20260423-224412.md`

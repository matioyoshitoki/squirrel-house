## 审查结论

审查结论: **NEEDS_MAJOR_FIX**

> **重要说明**：本次审查过程中，**未能获取 PR #58 的任何代码 diff**。经过多维度验证（GitHub API、远程分支、本地工作目录），确认 PR 内容不可达。本报告在"无法执行逐行 diff 审查"的前提下，基于 Issue #5 的公开需求边界、项目当前代码基线及已读取的规范文档，进行**范围审查（Scope Review）**和**合规性预检**。一旦 PR 代码可获取，必须进行完整的逐文件 diff 审查。

---

## 阻塞问题（Blocking）

### 1. PR 代码 diff 不可获取，核心审查无法进行
- **位置**: 工作目录 `/var/folders/lt/w3sl6z8j5v50vt2s6knvf5n00000gn/T/review-pr-58-3420212270`
- **问题**: 工作目录中仅包含空的 `.doc-coverage.txt`（无 PR 变更文件记录），无 `.git` 历史、无 patch/diff 文件、无源代码。GitHub API 返回 404（`Could not resolve to a PullRequest with the number of 58`）。远程仓库 `origin` 不存在 `feat/issue-5` 分支（`fatal: couldn't find remote ref feat/issue-5`）。
- **影响**: 无法执行代码规范性检查、功能完整性验证、测试覆盖评估等核心审查维度。
- **修复**: 
  1. 确认 PR #58 是否存在于其他 fork 或私有仓库；
  2. 若 PR 尚未推送，请将 `feat/issue-5` 分支推送至远程；
  3. 若审查文件生成失败，请重新触发 PR checkout 流程，确保变更文件落入审查工作目录。
- **依据**: `git branch -r` 输出、`gh pr view 58` 输出、工作目录 `find` 结果。

---

## 严重问题（Major）

### 2. 项目当前代码基线中 Issue #5 的两项目标均未实现，若 PR 未覆盖则功能不完整
- **位置**: `apps/phaser3/vite.config.ts:28-30`、`apps/phaser3/src/services/`、`apps/phaser3/src/api/`
- **问题**: Issue #5 明确要求：
  1. 修复 `vite.config.ts` 中 `copy-config-to-dist` 插件的 source 路径（`../../packages/configs` → `../../new-world-configs`）；
  2. 创建 `src/services/fish-service.ts`、`src/services/fish-types.ts`、`src/api/fish-api.ts` 等 Fish 模块前端代码。
  
经检查，当前 `main` 分支（`59bf152`）中：
  - `vite.config.ts` 第 28 行仍为 `const source = resolve(__dirname, '../../packages/configs');`，bug 未修复；
  - `src/services/` 下仅存在 `auth-service.ts` 和 `city-service.ts`，无 `fish-service.ts` 或 `fish-types.ts`；
  - `src/api/` 下仅存在 `auth-api.ts` 和 `building-api.ts`，无 `fish-api.ts`。
- **影响**: 若 PR #58 声称解决 Issue #5，但上述文件缺失，则功能验收不通过。
- **修复**: PR 必须包含：
  - `apps/phaser3/vite.config.ts` 中路径修正；
  - `apps/phaser3/src/api/fish-api.ts`（封装 `new-world-api/v1/fish.proto` 中全部 HTTP 接口）；
  - `apps/phaser3/src/services/fish-service.ts`（管理弟子列表、鱼苗池、缓存更新）；
  - `apps/phaser3/src/services/fish-types.ts`（`Fish`、`FishFry` 核心类型定义）；
  - 接入 `new-world-configs/` 下 6 份 CSV 配置（`fish_level_configs.csv`、`talent_configs.csv`、`personality_configs.csv`、`fish_looks_configs.csv`、`peculiarity_configs.csv`、`ling_gen_configs.csv`）。
- **依据**: GitHub Issue #5 描述、`apps/phaser3/vite.config.ts` 当前代码、`apps/phaser3/src/services/` 目录清单。

### 3. Fish 模块前端必须遵循前端分层 ESLint 约束
- **位置**: `apps/phaser3/.eslintrc.cjs:61-126`
- **问题**: 项目通过 `import/no-restricted-paths` 在 CI 级别强制前端分层。新增 `fish-api.ts` 和 `fish-service.ts` 时极易违反以下规则：
  - `api/` 层禁止依赖 `services/`、`scenes/`、`city-builder/`、`configs/`、`events/`；
  - `services/` 层禁止依赖 `scenes/`、`plugins/`、`managers/`；
  - `api/**/*.ts` 和 `services/**/*.ts` 额外开启 `@typescript-eslint/no-explicit-any: error` 和 `@typescript-eslint/explicit-function-return-type: warn`。
- **影响**: 若 PR 中 fish-api 导入了 `events/` 或 fish-service 导入了 `scenes/`，CI 将直接失败。
- **修复**: 
  - `fish-api.ts` 只允许导入 `../network/http` 和 `../proto/...`；
  - `fish-service.ts` 只允许导入 `../api/`、`../network/`、`../events/`、`../configs/`、`../city-builder/`；
  - 所有导出函数必须显式声明返回类型，禁止隐式 `any`。
- **依据**: `apps/phaser3/.eslintrc.cjs` 中 `zones` 配置与 `overrides` 规则。

---

## 轻微问题（Minor）

### 4. 若 PR 包含 Fish Service，需对齐现有 Service 风格
- **位置**: 预计 `apps/phaser3/src/services/fish-service.ts`
- **问题**: 现有 `auth-service.ts` 使用全静态方法 + 静态状态（`private static currentUser`），而 `city-service.ts` 使用实例方法 + 构造函数参数（`constructor(cityId: number = 1)`）。两种风格并存，但新模块应优先选择**实例化风格**，以便于多场景管理和单元测试 mock。
- **影响**: 代码风格不一致，增加维护成本。
- **修复**: 建议 `FishService` 采用实例化风格（参考 `CityService`），避免全局静态状态。若使用静态风格，需在模块文档中说明理由。
- **依据**: `apps/phaser3/src/services/city-service.ts:48-55`（实例化模式）；`apps/phaser3/src/services/auth-service.ts:24`（静态模式）。

### 5. 模块文档 `docs/modules/client/fish.md` 应同步创建或更新
- **位置**: `docs/modules/client/INDEX.md`
- **问题**: `docs/modules/client/INDEX.md` 的"模块覆盖状态"表中，`fish` 状态为 `⚠️ 部分`，说明为"仅有 protobuf 类型，无 API 封装和 gameplay Scene"。Issue #5 完成后，该文档应更新为 `✅ 完整` 并补充 `fish.md` 子文档。
- **影响**: 文档与代码状态不同步，违反 `AGENTS.md` 维护规则第 6 条（"新增/修改 proto、ent、配置、架构分层时，必须同步更新对应文档"）。
- **修复**: PR 合并前补充 `docs/modules/client/fish.md`，并更新 `docs/modules/client/INDEX.md` 中 fish 模块的状态和说明。
- **依据**: `docs/modules/client/INDEX.md:11`、`AGENTS.md:96-103`（维护规则）。

---

## 验证结果

| 维度 | 状态 | 说明 |
|------|------|------|
| 功能完整性 | ❌ | **未验证**。Issue #5 要求修复 vite 配置 + Fish 模块前端落地，但无法获取 PR 代码确认是否全部实现。项目当前代码基线中两项目标均未实现。 |
| 代码规范性 | ⚠️ | **部分预检**。已确认 ESLint 分层约束和 `no-explicit-any` / `explicit-function-return-type` 规则将适用于新增 Fish 模块代码，但无法检查实际代码是否合规。 |
| 全链路完整性 | ⚠️ | **未验证**。Fish 模块不涉及 DB/API/Mobile 全链路（属于前端模块），但需确认前端分层（API → Service → Scene）是否贯通。 |
| 规范符合性 | ⚠️ | **部分预检**。已读取 `agent-design-guide.md`、`testing.md`、`onboarding.md`。若 PR 包含 UI 组件，需额外对照 `PRD-004` 的命名规范、资源回退标准和动画标准。 |
| 测试覆盖 | ❌ | **未验证**。Issue #5 未明确要求 Fish 模块测试，但 `onboarding.md` 规定新增模块需通过 `pnpm --filter phaser3 test`。无法检查 PR 是否包含测试。 |
| 文档同步 | ⚠️ | **部分预检**。`docs/modules/client/INDEX.md` 尚未更新；若 PR 未包含文档更新，视为违规。 |

---

## 风险与建议

### 高优先级（PR 代码可获取后立即验证）
1. **vite 配置修复的副作用**: `../../new-world-configs` 目录在 monorepo 根目录已存在（`new-world-configs/`），路径修正后需验证 `pnpm build:web` 是否成功将配置复制到 `dist/config/`。建议 PR 中包含构建验证步骤或截图。
2. **Fish API 封装完整性**: `fish.proto` 已生成至 `src/proto/new-world-api/v1/fish.ts`（4737 行，接口众多），`fish-api.ts` 必须覆盖全部核心接口（如弟子列表、鱼苗池、移动、战斗、成长、死亡等）。建议对照 proto 中的全部 `Request` / `Result` 类型逐项检查，避免遗漏。
3. **Long 类型处理一致性**: 现有 `building-api.ts` 中大量使用 `Long.fromInt()` 和 `Long.fromString()` 进行 proto 字段转换。`fish-api.ts` 必须保持一致，避免直接传入 `number` 或 `string` 导致 protobuf 编码错误。

### 中优先级（合并前建议完成）
4. **配置加载器注册**: `fish-configs-helper.ts` 已存在（261 行），通过 `GlobalConfigsLoader.instance.getOne/getConfig` 读取 CSV。新增的 `fish-service.ts` 如需读取配置，应复用 `fishConfigsHelper` 单例，而非直接操作 `GlobalConfigsLoader`，以保持与现有模式一致。
5. **事件系统兼容性**: `fish-events.ts` 已定义 10 个 Fish 相关事件（如 `FishBeginBattleEvent`、`FishCoreChangeEvent`）。`fish-service.ts` 若需触发或监听这些事件，应确保事件类型与 `src/events/events/` 中定义的一致，避免重复定义或类型漂移。
6. **Personality Configs 接入**: Issue #5 要求接入 `personality_configs.csv`，但在 `fish-configs-helper.ts` 当前实现中未找到 `personality` 相关查询方法。若 PR 中包含 personality 配置读取逻辑，需确认 helper 已补充对应方法。

### 低优先级（后续迭代）
7. **测试策略**: 虽然 Issue #5 未强制要求单元测试，但 `docs/design-docs/testing.md` 要求关键组件（状态机、数值计算、边界验证）达到 100% 覆盖率。`fish-service.ts` 中的缓存更新逻辑、状态筛选逻辑属于"状态机逻辑"范畴，建议在后续迭代中补充测试。
8. **前端 E2E 验收**: 若 Fish 模块包含 UI Scene，需遵循 `docs/frontend/e2e-testing.md` 和 `docs/design-docs/visual-qa-sop.md`，产出 Playwright 截图并通过视觉验收。

---

## 审查依据文档清单

| 文档 | 路径 | 用途 |
|------|------|------|
| AGENTS.md | `/Users/insulate/Desktop/new-world-monorepo/AGENTS.md` | 项目导航、维护规则 |
| PRD-004 | `/Users/insulate/Desktop/new-world-monorepo/prd/PRD-004-ui-component-library-redesign.md` | UI 命名规范、动画标准、深度规范（若 PR 涉及 UI 组件） |
| PRD-003 | `/Users/insulate/Desktop/new-world-monorepo/prd/PRD-003-leaderboard-system.md` | 关联参考（本次 PR 不直接涉及） |
| 前端 UI 开发规范 | `/Users/insulate/Desktop/new-world-monorepo/docs/frontend/agent-design-guide.md` | 前端代码规范、素材检索、截图验收 |
| 测试规范 | `/Users/insulate/Desktop/new-world-monorepo/docs/design-docs/testing.md` | 测试策略、覆盖率要求 |
| 新模块开发指南 | `/Users/insulate/Desktop/new-world-monorepo/docs/design-docs/onboarding.md` | 前端模块创建流程、分层规范、ESLint 约束 |
| 客户端模块索引 | `/Users/insulate/Desktop/new-world-monorepo/docs/modules/client/INDEX.md` | 模块覆盖状态、验收清单 |
| 城建模块文档 | `/Users/insulate/Desktop/new-world-monorepo/docs/modules/client/city-building.md` | 参考现有模块的文档格式和接口清单 |
| ESLint 配置 | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/.eslintrc.cjs` | 分层依赖约束、类型严格规则 |
| vite 配置 | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/vite.config.ts` | Issue #5 要求修复的目标文件 |
| fish proto | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/src/proto/new-world-api/v1/fish.ts` | API 封装需对齐的 proto 类型 |
| fish config helper | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/src/configs/helpers/fish-configs-helper.ts` | 现有配置读取基础设施 |
| fish events | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/src/events/events/fish-events.ts` | 现有事件定义 |
| auth/building API | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/src/api/auth-api.ts`、`building-api.ts` | API 封装风格参考 |
| auth/building Service | `/Users/insulate/Desktop/new-world-monorepo/apps/phaser3/src/services/auth-service.ts`、`city-service.ts` | Service 层风格参考 |

---

*本报告由 Code Review Agent 基于可用信息生成。由于 PR #58 的代码 diff 不可获取，报告中标记为"未验证"的维度需在代码可达后进行补充审查。*
